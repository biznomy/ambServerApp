<div class="row-fluid  ui-sortable">
        <div style="" class="box span12">
          <div class="box-header" data-original-title="">
            <h2><i class="icon-edit"></i><span class="break"></span>Vehicles</h2>
            <!--<div class="box-icon">
              
              <a href="#" class="btn-close"><i class="icon-remove"></i></a>
            </div>-->
          </div>
<div class="box-content">




<%= form_for([:admin,@vendor,@vehicle], :html => { :class => "form-horizontal"}) do |f| %>
  <% if @vehicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vehicle.errors.count, "error") %> prohibited this vehicle from being saved:</h2>

      <ul>
      <% @vehicle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<fieldset>
  <div class="control-group">
    <%= f.label :number,:class=>"control-label" %>
    <div class="controls">
    <%= f.text_field :number,:class=>"span6" %>
    <% if current_admin.role =='vendor' %>
    <%= f.hidden_field(:vendor_id, :value =>Vendor.where(:admin_id=>current_admin.id).collect {|p| p.id  }*",") %>
<% else %>
    <%= f.hidden_field :vendor_id, :value => params[:vendor_id] %>
    <% end %>
  </div>
  </div>
  <!--<div class="control-group">
  <label class="control-label">Vehicle  Addon</label>
  <div class="controls">
 <label class="checkbox inline">
  <%#= f.collection_check_boxes(:addon_ids, Addon.all, :id, :name ) %>
     <%#= f.collection_check_boxes(:addon_ids, Addon.all, :id, :name) do |b| %>
        <%#= b.label class:"checkbox inline" do%>
         <%#=b.check_box + b.text%>
        <%#end%>
    <%# end %>
</label>
</div>
</div>-->
    <div class="form-actions">
     <%= f.submit "Save",:class=>"btn btn-primary" %>
    </div>
</fieldset>
<% end %>
</div>
</div>
</div>