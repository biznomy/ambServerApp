<div class="row-fluid ui-sortable">
        <div style="" class="box span12">
          <div class="box-header" data-original-title="">
            <h2><i class="icon-edit"></i><span class="break"></span>Addons</h2>
            <div class="box-icon">
              
              <a href="#" class="btn-close"><i class="icon-remove"></i></a>
            </div>
          </div>
<div class="box-content">
<%= form_for([:admin,@addon], :html => { :class => "form-horizontal"}) do |f| %>
  <% if @addon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@addon.errors.count, "error") %> prohibited this addon from being saved:</h2>
      <ul>
      <% @addon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<fieldset>
<div class="control-group">
    <%= f.label :name,:class=>"control-label" %>
    <div class="controls">
    <%= f.text_field :name,:class=>"span6" %>
  </div>
  </div>
  <div class="form-actions">
    <%= f.submit "Save",:class=>"btn btn-primary" %>
  </div>
 </fieldset>
<% end %>
</div>
</div>
</div>
