<%= form_for(@driverprofile) do |f| %>
  <% if @driverprofile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@driverprofile.errors.count, "error") %> prohibited this driverprofile from being saved:</h2>

      <ul>
      <% @driverprofile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
